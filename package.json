{
  "name": "pmsg-india-solar",
  "version": "1.0.0",
  "description": "PM Surya Ghar rooftop solar vendor registry — multi-state India dashboard",
  "private": true,
  "engines": { "node": ">=22.5" },
  "scripts": {
    "start":          "node start.js",
    "auto":           "node start.js --auto --now",
    "ingest":         "node src/ingest.js",
    "export":         "node src/export-static.js --redact",
    "export:full":    "node src/export-static.js",
    "harvest":        "node src/auto-harvest.js",
    "harvest:watch":  "node src/auto-harvest.js --headed",
    "ci":             "node src/ci-run.js --redact"
  },
  "optionalDependencies": {
    "playwright": "^1.47.0"
  }
}
